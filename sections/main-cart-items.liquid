{{ 'component-cart.css' | asset_url | stylesheet_tag }}
{{ 'component-cart-items.css' | asset_url | stylesheet_tag }}
{{ 'component-totals.css' | asset_url | stylesheet_tag }}
{{ 'component-price.css' | asset_url | stylesheet_tag }}
{{ 'component-discounts.css' | asset_url | stylesheet_tag }}
{{ 'quantity-popover.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  /* Hide default Dawn duplicate cart footer */
  .cart__footer-wrapper,
  .cart__footer,
  #main-cart-footer,
  .main-cart-footer {
    display: none !important;
  }
{%- endstyle -%}

<script src="{{ 'quantity-popover.js' | asset_url }}" defer="defer"></script>

<div class="page-width section-{{ section.id }}-padding meeeku-cart-page-container">

  <!-- Smart Meeeku Cart Container -->
  <div id="MeeekuLocalCartWrapper" class="meeeku-cart-wrapper" style="display: none;">
    
    <!-- Cart Page Header -->
    <div class="meeeku-cart-header">
      <div style="display: flex; align-items: center; gap: 0.8rem; flex-wrap: wrap;">
        <h1 class="meeeku-cart-title">
          🛍️ Your Meeeku Shopping Bag
        </h1>
        <span id="MeeekuCartCountBadge" class="meeeku-cart-count-badge">
          0 Items
        </span>
      </div>
      <a href="/collections/all" class="meeeku-cart-continue-link">
        ← Continue Shopping
      </a>
    </div>

    <!-- Free Delivery Progress Bar (Threshold: ₹1,000) -->
    <div id="MeeekuPageShippingProgress" style="background: #FFF5F8; border: 1.5px solid #F8BBD0; border-radius: 16px; padding: 1.4rem 1.8rem; margin-bottom: 2rem;">
      <div id="MeeekuPageShippingMsg" style="font-size: 1.4rem; font-weight: 700; color: #3D2355; margin-bottom: 0.8rem; text-align: center; line-height: 1.4;">
        Add ₹1,000.00 more to unlock FREE Express Delivery! 🚚
      </div>
      <div style="width: 100%; height: 12px; background: #FFE0EC; border-radius: 10px; overflow: hidden; position: relative;">
        <div id="MeeekuPageShippingFill" style="width: 0%; height: 100%; background: linear-gradient(90deg, #FF6097, #6B3FA0); border-radius: 10px; transition: width 0.4s ease, background 0.4s ease;"></div>
      </div>
    </div>

    <!-- Available Discounts & Reminders Container for Cart Page -->
    <div id="MeeekuPageDiscountsBox" class="meeeku-cart-discounts-box">
      <div class="meeeku-cart-discounts-header">
        <div style="display: flex; align-items: center; gap: 0.6rem;">
          <span style="font-size: 1.6rem;">🏷️</span>
          <span class="meeeku-cart-discounts-title">Available Discounts & Offers</span>
        </div>
        <span class="meeeku-cart-discounts-sub">Tap to apply at checkout</span>
      </div>

      <div class="meeeku-page-discounts-grid">
        <!-- Card 1: WELCOME20 -->
        <div class="meeeku-cart-discount-card" id="MeeekuPageCard-WELCOME20" onclick="meeekuApplyCartDiscount('WELCOME20', this)">
          <div class="meeeku-cart-discount-badge">20% OFF</div>
          <div class="meeeku-cart-discount-info">
            <div class="meeeku-cart-discount-code">WELCOME20</div>
            <div class="meeeku-cart-discount-text">20% OFF your first order site-wide</div>
          </div>
          <button type="button" class="meeeku-cart-discount-btn" id="MeeekuPageBtn-WELCOME20">Apply</button>
        </div>

        <!-- Card 2: B2G1 -->
        <div class="meeeku-cart-discount-card" id="MeeekuPageCard-B2G1" onclick="meeekuApplyCartDiscount('B2G1', this)">
          <div class="meeeku-cart-discount-badge" style="background: #FFE082; color: #E65100;">BOGO DEAL</div>
          <div class="meeeku-cart-discount-info">
            <div class="meeeku-cart-discount-code">B2G1</div>
            <div class="meeeku-cart-discount-text">Buy 2 Get 1 FREE on Tees & Co-ords</div>
          </div>
          <button type="button" class="meeeku-cart-discount-btn" id="MeeekuPageBtn-B2G1">Apply</button>
        </div>
      </div>

      <div id="MeeekuPageDiscountAppliedAlert" class="meeeku-discount-applied-alert" style="display: none;">
        <span id="MeeekuPageDiscountAppliedMsg">🎉 Code <strong>WELCOME20</strong> applied!</span>
        <button type="button" class="meeeku-discount-remove-btn" onclick="meeekuRemoveCartDiscount(event)">Remove</button>
      </div>
    </div>

    <!-- Product Rows Container -->
    <div id="MeeekuLocalCartList" class="meeeku-cart-items-list"></div>

    <!-- Order Summary & Checkout Section -->
    <div class="meeeku-cart-summary-wrapper">
      <div class="meeeku-cart-summary-grid">
        
        <!-- Left Column: Trust & Delivery Benefit -->
        <div class="meeeku-cart-shipping-box" id="MeeekuPageTrustShippingBox">
          <div style="font-size: 2.4rem; flex-shrink: 0;">🚚</div>
          <div>
            <div id="MeeekuPageTrustTitle" style="color: #2E7D32; font-size: 1.35rem; font-weight: 800;">Free Shipping Across India</div>
            <div id="MeeekuPageTrustSubtitle" style="color: #555; font-size: 1.2rem; margin-top: 0.2rem;">Delivered in 3–5 business days • 7-day hassle-free exchanges</div>
          </div>
        </div>

        <!-- Right Column: Subtotal & Actions -->
        <div class="meeeku-cart-checkout-box">
          <div style="display: flex; justify-content: space-between; align-items: center; font-size: 1.45rem; color: #666; margin-bottom: 0.5rem; width: 100%;">
            <span>Bag Subtotal:</span>
            <span id="MeeekuPageSubtotal" style="color: #3D2355; font-weight: 700; font-size: 1.55rem;">₹0.00</span>
          </div>
          <div id="MeeekuPageGiftRow" style="display: none; justify-content: space-between; align-items: center; font-size: 1.45rem; margin-bottom: 0.5rem; width: 100%; background: #F3E5F5; padding: 0.5rem 1rem; border-radius: 12px; border: 1px dashed #CE93D8;">
            <span style="color: #6A1B9A; font-weight: 700;">🎁 Surprise Gift (Little Ones):</span>
            <span style="font-weight: 800; font-size: 1.35rem; color: #2E7D32;">FREE ✨</span>
          </div>
          <div style="display: flex; justify-content: space-between; align-items: center; font-size: 1.45rem; margin-bottom: 0.8rem; width: 100%;">
            <span>Delivery Charge:</span>
            <span id="MeeekuPageDeliveryFee" style="font-weight: 700; font-size: 1.45rem; color: #2E7D32;">FREE</span>
          </div>
          <div class="meeeku-cart-subtotal-row" style="border-top: 1px solid #FFE0EC; padding-top: 0.8rem; width: 100%;">
            <span style="font-size: 1.6rem; color: #3D2355; font-weight: 800;">Estimated Total:</span>
            <span id="MeeekuLocalCartTotal" class="meeeku-cart-total-price">₹0.00</span>
          </div>

          <div class="meeeku-cart-buttons-row">
            <button type="button" class="button button--secondary meeeku-btn-clear" onclick="meeekuClearCart()">
              Clear Bag
            </button>
            <button type="button" class="button button--primary meeeku-btn-checkout" onclick="meeekuProceedToCheckout()">
              🔒 Checkout
            </button>
          </div>
        </div>

      </div>
    </div>

  </div>

  <!-- Empty State Fallback -->
  <div id="MeeekuCartEmptyState" class="cart__warnings" style="display: block; text-align: center; padding: 5rem 1rem;">
    <div style="font-size: 4.5rem; margin-bottom: 1.2rem;">🛍️</div>
    <h2 class="cart__empty-text" style="color: #3D2355; font-size: 2.4rem; font-weight: 800; margin-bottom: 0.8rem;">Your Meeeku shopping bag is empty</h2>
    <p style="font-size: 1.5rem; color: #777; margin-bottom: 1.6rem; max-width: 380px; margin-left: auto; margin-right: auto;">
      Discover our 100% skin-safe organic cotton tees, 2-piece co-ord sets, and handcrafted ethnic lehengas!
    </p>
    <div style="background: #FFF3E0; border: 1.5px dashed #FFB74D; border-radius: 14px; padding: 1rem 1.6rem; margin-bottom: 2.2rem; max-width: 400px; margin-left: auto; margin-right: auto;">
      <span style="font-size: 1.35rem; color: #E65100; font-weight: 700;">✨ Tip: Get <strong>20% OFF</strong> your first order with code <strong style="text-decoration: underline;">WELCOME20</strong>!</span>
    </div>
    <a href="/collections/all" class="button button--primary" style="background: #6B3FA0; border-color: #6B3FA0; border-radius: 30px; padding: 1.2rem 3rem; font-size: 1.5rem; font-weight: 700; text-decoration: none;">
      Explore Collections ✨
    </a>
  </div>

</div>

<style>
.meeeku-cart-page-container {
  max-width: 1000px;
  margin: 0 auto;
  padding-left: 1.5rem;
  padding-right: 1.5rem;
}

.meeeku-cart-wrapper {
  background: #FFFDF7;
  border: 2px solid #F8BBD0;
  border-radius: 24px;
  padding: 2.5rem;
  box-shadow: 0 10px 30px rgba(107, 63, 160, 0.08);
  margin-top: 1rem;
}

.meeeku-cart-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-bottom: 2px solid #FCE4EC;
  padding-bottom: 1.8rem;
  margin-bottom: 2.2rem;
  flex-wrap: wrap;
  gap: 1rem;
}

.meeeku-cart-title {
  color: #3D2355;
  font-size: 2.4rem;
  font-weight: 800;
  margin: 0;
  display: flex;
  align-items: center;
  gap: 0.8rem;
}

.meeeku-cart-count-badge {
  background: #EDE7F6;
  color: #6B3FA0;
  font-size: 1.25rem;
  font-weight: 700;
  padding: 0.3rem 1rem;
  border-radius: 20px;
}

.meeeku-cart-continue-link {
  color: #6B3FA0;
  font-size: 1.35rem;
  font-weight: 700;
  text-decoration: none;
  border-bottom: 1.5px dashed #F8BBD0;
  padding-bottom: 0.2rem;
  transition: all 0.2s ease;
}

.meeeku-cart-continue-link:hover {
  color: #FF6097;
  border-color: #FF6097;
}

.meeeku-cart-items-list {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  margin-bottom: 2.5rem;
}

.meeeku-cart-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: #FFF;
  border: 1.5px solid #F8BBD0;
  border-radius: 20px;
  padding: 1.6rem 2rem;
  box-shadow: 0 4px 14px rgba(107, 63, 160, 0.04);
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.meeeku-cart-row:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(107, 63, 160, 0.08);
}

.meeeku-cart-row-left {
  display: flex;
  align-items: center;
  gap: 1.8rem;
  flex: 1;
}

.meeeku-cart-item-img {
  width: 90px;
  height: 110px;
  object-fit: cover;
  border-radius: 14px;
  border: 1.5px solid #FFE0EC;
  flex-shrink: 0;
}

.meeeku-cart-row-right {
  display: flex;
  align-items: center;
  gap: 2.5rem;
}

.meeeku-cart-summary-wrapper {
  border-top: 2px dashed #F8BBD0;
  padding-top: 2.2rem;
  margin-top: 1rem;
}

.meeeku-cart-summary-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 2rem;
  align-items: center;
}

.meeeku-cart-shipping-box {
  display: flex;
  align-items: center;
  gap: 1.2rem;
  background: #F1F8E9;
  border: 1.5px solid #C8E6C9;
  border-radius: 18px;
  padding: 1.4rem 1.6rem;
}

.meeeku-cart-checkout-box {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 1.2rem;
}

.meeeku-cart-subtotal-row {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  width: 100%;
  max-width: 360px;
}

.meeeku-cart-total-price {
  font-size: 2.5rem;
  font-weight: 800;
  color: #6B3FA0;
}

.meeeku-cart-buttons-row {
  display: flex;
  gap: 1rem;
  width: 100%;
  max-width: 360px;
}

.meeeku-btn-clear {
  flex: 1;
  border-radius: 30px;
  padding: 1.1rem;
  font-size: 1.35rem;
  border-color: #F8BBD0;
  color: #6B3FA0;
  font-weight: 700;
  background: #FFF;
  cursor: pointer;
  white-space: nowrap;
}

.meeeku-btn-checkout {
  flex: 2;
  border-radius: 30px;
  padding: 1.2rem 1.5rem;
  font-size: 1.5rem;
  font-weight: 800;
  background: linear-gradient(135deg, #FF6097, #E91E63);
  border: none;
  box-shadow: 0 4px 16px rgba(255, 96, 151, 0.4);
  color: #FFF;
  cursor: pointer;
  white-space: nowrap;
}

/* Meeeku Cart Page Discounts Styling */
.meeeku-cart-discounts-box {
  background: #FFF9FA;
  border: 1.5px solid #F8BBD0;
  border-radius: 18px;
  padding: 1.4rem 1.8rem;
  margin-bottom: 2rem;
}
.meeeku-cart-discounts-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 1.2rem;
  flex-wrap: wrap;
  gap: 0.5rem;
}
.meeeku-cart-discounts-title {
  font-size: 1.55rem;
  font-weight: 800;
  color: #3D2355;
}
.meeeku-cart-discounts-sub {
  font-size: 1.25rem;
  color: #FF6097;
  font-weight: 700;
}
.meeeku-page-discounts-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 1.2rem;
}
.meeeku-cart-discount-card {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: #FFFFFF;
  border: 1.5px dashed #F8BBD0;
  border-radius: 14px;
  padding: 1rem 1.4rem;
  cursor: pointer;
  transition: all 0.25s ease;
}
.meeeku-cart-discount-card:hover {
  border-color: #FF6097;
  box-shadow: 0 4px 16px rgba(255, 96, 151, 0.18);
  transform: translateY(-2px);
}
.meeeku-cart-discount-card.applied {
  border-color: #2E7D32;
  background: #F1F8E9;
}
.meeeku-cart-discount-badge {
  background: #FFD54F;
  color: #3D2355;
  font-size: 1.15rem;
  font-weight: 800;
  padding: 0.3rem 0.75rem;
  border-radius: 12px;
  margin-right: 1rem;
  white-space: nowrap;
}
.meeeku-cart-discount-info {
  flex: 1;
  display: flex;
  flex-direction: column;
}
.meeeku-cart-discount-code {
  font-size: 1.45rem;
  font-weight: 800;
  color: #3D2355;
  letter-spacing: 0.05em;
}
.meeeku-cart-discount-text {
  font-size: 1.2rem;
  color: #666;
  line-height: 1.35;
}
.meeeku-cart-discount-btn {
  background: #FFF;
  border: 1.5px solid #FF6097;
  color: #FF6097;
  font-size: 1.25rem;
  font-weight: 800;
  border-radius: 20px;
  padding: 0.45rem 1.2rem;
  cursor: pointer;
  transition: all 0.2s ease;
  white-space: nowrap;
}
.meeeku-cart-discount-btn:hover,
.meeeku-cart-discount-card.applied .meeeku-cart-discount-btn {
  background: #2E7D32;
  border-color: #2E7D32;
  color: #FFF;
}
.meeeku-discount-applied-alert {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: #E8F5E9;
  border: 1px solid #A5D6A7;
  border-radius: 12px;
  padding: 0.8rem 1.4rem;
  margin-top: 1.2rem;
  font-size: 1.3rem;
  color: #1B5E20;
  font-weight: 700;
}
.meeeku-discount-remove-btn {
  background: transparent;
  border: none;
  color: #C62828;
  font-weight: 800;
  cursor: pointer;
  padding: 0.2rem 0.5rem;
  font-size: 1.3rem;
  text-decoration: underline;
}

@media screen and (max-width: 749px) {
  .meeeku-cart-page-container {
    padding-left: 1.2rem;
    padding-right: 1.2rem;
  }
  .meeeku-cart-wrapper {
    padding: 1.4rem 1.2rem;
    border-radius: 18px;
    margin-top: 0.5rem;
  }
  .meeeku-cart-title {
    font-size: 1.9rem;
  }
  .meeeku-cart-header {
    padding-bottom: 1.2rem;
    margin-bottom: 1.5rem;
  }
  .meeeku-cart-row {
    flex-direction: column;
    align-items: stretch;
    gap: 1.2rem;
    padding: 1.2rem;
    border-radius: 16px;
  }
  .meeeku-cart-row-left {
    width: 100%;
  }
  .meeeku-cart-item-img {
    width: 75px;
    height: 92px;
  }
  .meeeku-cart-row-right {
    width: 100%;
    justify-content: space-between;
    border-top: 1px dashed #FCE4EC;
    padding-top: 0.9rem;
  }
  .meeeku-cart-summary-grid {
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }
  .meeeku-cart-checkout-box {
    align-items: stretch;
  }
  .meeeku-cart-subtotal-row {
    max-width: 100%;
  }
  .meeeku-cart-total-price {
    font-size: 2.2rem;
  }
  .meeeku-cart-buttons-row {
    max-width: 100%;
  }
  .meeeku-btn-checkout {
    padding: 1.1rem;
    font-size: 1.4rem;
  }
}
</style>

<script>
function formatMeeekuPaise(paise) {
  var rupees = Math.round(paise / 100);
  return '₹' + rupees.toLocaleString('en-IN') + '.00';
}

function parseMeeekuPrice(priceStr) {
  if (typeof priceStr === 'number') return priceStr;
  var num = parseFloat(String(priceStr || '0').replace(/[^0-9.]/g, ''));
  return isNaN(num) ? 0 : num;
}

function updateCartBadges(count) {
  var c = parseInt(count) || 0;
  var countBubbles = document.querySelectorAll('.cart-count-bubble span');
  countBubbles.forEach(function(b) { b.textContent = c; });
  var dot = document.getElementById('MeeekuCartDot');
  if (dot) {
    if (c > 0) dot.classList.add('has-items');
    else dot.classList.remove('has-items');
  }
}

function updateMeeekuPageShippingProgress(subtotalRupees) {
  var box = document.getElementById('MeeekuPageShippingProgress');
  var msg = document.getElementById('MeeekuPageShippingMsg');
  var fill = document.getElementById('MeeekuPageShippingFill');
  var deliveryEl = document.getElementById('MeeekuPageDeliveryFee');
  var totalEl = document.getElementById('MeeekuLocalCartTotal');
  var trustTitle = document.getElementById('MeeekuPageTrustTitle');
  var trustSubtitle = document.getElementById('MeeekuPageTrustSubtitle');

  var subtotal = parseFloat(subtotalRupees) || 0;
  if (!box || !msg || !fill) return;

  if (subtotal <= 0) {
    box.style.display = 'none';
    return;
  }

  box.style.display = 'block';
  var threshold = 1000;
  var deliveryFee = 0;

  if (subtotal < threshold) {
    deliveryFee = 100;
    var diff = threshold - subtotal;
    var pct = Math.min(100, Math.max(8, (subtotal / threshold) * 100));
    msg.innerHTML = 'Add <strong style="color: #FF6097;">₹' + diff.toLocaleString('en-IN', {minimumFractionDigits: 2}) + '</strong> more for <span style="color: #2E7D32; font-weight: 800;">FREE Delivery + FREE Gift!</span> 🎁🚚';
    fill.style.width = pct + '%';
    fill.style.background = 'linear-gradient(90deg, #FF6097, #6B3FA0)';
    if (deliveryEl) {
      deliveryEl.innerText = '₹100.00';
      deliveryEl.style.color = '#E65100';
    }
    if (trustTitle) trustTitle.innerText = 'Standard Delivery (₹100)';
    if (trustSubtitle) trustSubtitle.innerText = 'Add ₹' + diff.toLocaleString('en-IN', {minimumFractionDigits: 2}) + ' more to get Free Delivery & Surprise Gift!';
    var giftRow = document.getElementById('MeeekuPageGiftRow');
    if (giftRow) giftRow.style.display = 'none';
  } else {
    deliveryFee = 0;
    msg.innerHTML = '🎉 <strong style="color: #2E7D32;">Unlocked FREE Express Delivery + FREE Surprise Gift Inside! 🎁</strong>';
    fill.style.width = '100%';
    fill.style.background = 'linear-gradient(90deg, #4CAF50, #2E7D32)';
    if (deliveryEl) {
      deliveryEl.innerText = 'FREE (₹0.00)';
      deliveryEl.style.color = '#2E7D32';
    }
    if (trustTitle) trustTitle.innerText = '🎁 Free Surprise Gift Included + Free Shipping';
    if (trustSubtitle) trustSubtitle.innerText = 'Surprise gift for the little one included inside • Delivered in 3–5 business days';
    var giftRow = document.getElementById('MeeekuPageGiftRow');
    if (giftRow) giftRow.style.display = 'flex';
  }

  if (totalEl) {
    var finalTotal = subtotal + deliveryFee;
    totalEl.innerText = '₹' + finalTotal.toLocaleString('en-IN', {minimumFractionDigits: 2});
  }
}

function fetchMeeekuPageCart(callback) {
  fetch('/cart.js', { cache: 'no-store' })
    .then(function(res) {
      if (!res.ok) throw new Error('Status ' + res.status);
      return res.json();
    })
    .then(function(cart) {
      renderMeeekuPageCartWithData(cart);
      updateCartBadges(cart.item_count || 0);
      if (typeof callback === 'function') callback(cart);
    })
    .catch(function(err) {
      renderMeeekuPageCartWithLocalFallback();
      if (typeof callback === 'function') callback(null);
    });
}

function renderMeeekuPageCartWithLocalFallback() {
  var wrapper = document.getElementById('MeeekuLocalCartWrapper');
  var emptyState = document.getElementById('MeeekuCartEmptyState');
  var list = document.getElementById('MeeekuLocalCartList');
  var badgeEl = document.getElementById('MeeekuCartCountBadge');
  var subtotalEl = document.getElementById('MeeekuPageSubtotal');

  if (!wrapper || !emptyState || !list) return;

  var localCart = [];
  try {
    localCart = JSON.parse(localStorage.getItem('meeeku_active_cart') || localStorage.getItem('meeeku_user_cart') || localStorage.getItem('meeeku_cart') || '[]');
  } catch(e) {}

  if (localCart.length === 0) {
    wrapper.style.display = 'none';
    emptyState.style.display = 'block';
    updateCartBadges(0);
    return;
  }

  emptyState.style.display = 'none';
  wrapper.style.display = 'block';

  var totalCount = 0;
  var subtotal = 0;
  var html = '';

  localCart.forEach(function(item, idx) {
    var key = item.key || item.id || ('local_' + idx);
    var qty = parseInt(item.quantity || item.qty) || 1;
    totalCount += qty;
    var unitNum = parseMeeekuPrice(item.price);
    var lineTotal = unitNum * qty;
    subtotal += lineTotal;

    var unitPriceFormatted = '₹' + unitNum.toLocaleString('en-IN', {minimumFractionDigits: 2});
    var lineTotalFormatted = '₹' + lineTotal.toLocaleString('en-IN', {minimumFractionDigits: 2});
    var imgSrc = item.img || item.image || 'https://raw.githubusercontent.com/aaybwork-studio/meeeku/main/assets/meeeku_prod_yellow_lehenga.jpg';
    var sizeLabel = item.size || 'Standard';

    html += [
      '<div class="meeeku-cart-row">',
        '<div class="meeeku-cart-row-left">',
          '<img src="' + imgSrc + '" alt="' + (item.title || 'Meeeku Garment') + '" class="meeeku-cart-item-img">',
          '<div style="display: flex; flex-direction: column; gap: 0.35rem; min-width: 0;">',
            '<h3 style="margin: 0; color: #3D2355; font-size: 1.55rem; font-weight: 800; line-height: 1.3; word-break: break-word;">' + (item.title || 'Meeeku Garment') + '</h3>',
            '<div style="display: flex; align-items: center; gap: 0.5rem; margin-top: 0.15rem;">',
              '<span style="font-size: 1.2rem; color: #666; font-weight: 600;">Size:</span>',
              '<span style="border: 1.5px solid #F8BBD0; background: #FFFDF7; color: #6B3FA0; font-weight: 700; font-size: 1.2rem; padding: 0.2rem 0.7rem; border-radius: 10px;">' + sizeLabel + '</span>',
            '</div>',
            '<div style="font-size: 1.25rem; color: #888; font-weight: 600;">',
              'Unit Price: <span style="color: #6B3FA0; font-weight: 700;">' + unitPriceFormatted + '</span>',
            '</div>',
          '</div>',
        '</div>',
        '<div class="meeeku-cart-row-right">',
          '<div style="display: flex; align-items: center; background: #FFFDF7; border: 1.5px solid #F8BBD0; border-radius: 25px; padding: 0.3rem 0.8rem; box-shadow: 0 2px 6px rgba(107,63,160,0.05);">',
            '<button type="button" onclick="meeekuUpdatePageQty(\'' + key + '\', ' + (qty - 1) + ')" style="background: none; border: none; font-size: 1.7rem; font-weight: 800; color: #FF6097; cursor: pointer; padding: 0 0.4rem; line-height: 1;" title="Decrease quantity">−</button>',
            '<span style="font-size: 1.4rem; font-weight: 800; padding: 0 0.8rem; color: #3D2355; min-width: 20px; text-align: center;">' + qty + '</span>',
            '<button type="button" onclick="meeekuUpdatePageQty(\'' + key + '\', ' + (qty + 1) + ')" style="background: none; border: none; font-size: 1.7rem; font-weight: 800; color: #FF6097; cursor: pointer; padding: 0 0.4rem; line-height: 1;" title="Increase quantity">+</button>',
          '</div>',
          '<div style="font-size: 1.75rem; font-weight: 800; color: #3D2355; text-align: right;">' + lineTotalFormatted + '</div>',
          '<button type="button" onclick="meeekuRemovePageItem(\'' + key + '\')" style="background: #FFF0F5; border: 1.5px solid #F8BBD0; color: #C2185B; border-radius: 50%; width: 34px; height: 34px; cursor: pointer; font-size: 1.3rem; display: flex; align-items: center; justify-content: center; transition: all 0.2s ease; flex-shrink: 0;" title="Remove from bag">✕</button>',
        '</div>',
      '</div>'
    ].join('');
  });

  list.innerHTML = html;
  if (subtotalEl) {
    subtotalEl.innerText = '₹' + subtotal.toLocaleString('en-IN', {minimumFractionDigits: 2});
  }
  updateMeeekuPageShippingProgress(subtotal);
  if (badgeEl) {
    badgeEl.innerText = totalCount + (totalCount === 1 ? ' Item' : ' Items');
  }
  updateCartBadges(totalCount);
}

function renderMeeekuPageCartWithData(cart) {
  var wrapper = document.getElementById('MeeekuLocalCartWrapper');
  var emptyState = document.getElementById('MeeekuCartEmptyState');
  var list = document.getElementById('MeeekuLocalCartList');
  var badgeEl = document.getElementById('MeeekuCartCountBadge');
  var subtotalEl = document.getElementById('MeeekuPageSubtotal');

  if (!wrapper || !emptyState || !list) return;

  var items = (cart && cart.items) ? cart.items : [];

  if (items.length === 0) {
    renderMeeekuPageCartWithLocalFallback();
    return;
  }

  emptyState.style.display = 'none';
  wrapper.style.display = 'block';

  var html = '';

  items.forEach(function(item) {
    var itemKey = item.key || item.id;
    var unitPriceFormatted = formatMeeekuPaise(item.final_price || item.price || 0);
    var lineTotalFormatted = formatMeeekuPaise(item.final_line_price || item.line_price || 0);
    var imgSrc = item.image || (item.featured_image ? item.featured_image.url : 'https://raw.githubusercontent.com/aaybwork-studio/meeeku/main/assets/meeeku_prod_yellow_lehenga.jpg');
    var variantTitle = item.variant_title ? item.variant_title : '';

    html += [
      '<div class="meeeku-cart-row">',
        '<div class="meeeku-cart-row-left">',
          '<img src="' + imgSrc + '" alt="' + (item.product_title || item.title) + '" class="meeeku-cart-item-img">',
          '<div style="display: flex; flex-direction: column; gap: 0.35rem; min-width: 0;">',
            '<h3 style="margin: 0; color: #3D2355; font-size: 1.55rem; font-weight: 800; line-height: 1.3; word-break: break-word;">' + (item.product_title || item.title) + '</h3>',
            (variantTitle ? [
              '<div style="display: flex; align-items: center; gap: 0.5rem; margin-top: 0.15rem;">',
                '<span style="font-size: 1.2rem; color: #666; font-weight: 600;">Size:</span>',
                '<span style="border: 1.5px solid #F8BBD0; background: #FFFDF7; color: #6B3FA0; font-weight: 700; font-size: 1.2rem; padding: 0.2rem 0.7rem; border-radius: 10px;">' + variantTitle + '</span>',
              '</div>'
            ].join('') : ''),
            '<div style="font-size: 1.25rem; color: #888; font-weight: 600;">',
              'Unit Price: <span style="color: #6B3FA0; font-weight: 700;">' + unitPriceFormatted + '</span>',
            '</div>',
          '</div>',
        '</div>',
        '<div class="meeeku-cart-row-right">',
          '<div style="display: flex; align-items: center; background: #FFFDF7; border: 1.5px solid #F8BBD0; border-radius: 25px; padding: 0.3rem 0.8rem; box-shadow: 0 2px 6px rgba(107,63,160,0.05);">',
            '<button type="button" onclick="meeekuUpdatePageQty(\'' + itemKey + '\', ' + (item.quantity - 1) + ')" style="background: none; border: none; font-size: 1.7rem; font-weight: 800; color: #FF6097; cursor: pointer; padding: 0 0.4rem; line-height: 1;" title="Decrease quantity">−</button>',
            '<span style="font-size: 1.4rem; font-weight: 800; padding: 0 0.8rem; color: #3D2355; min-width: 20px; text-align: center;">' + item.quantity + '</span>',
            '<button type="button" onclick="meeekuUpdatePageQty(\'' + itemKey + '\', ' + (item.quantity + 1) + ')" style="background: none; border: none; font-size: 1.7rem; font-weight: 800; color: #FF6097; cursor: pointer; padding: 0 0.4rem; line-height: 1;" title="Increase quantity">+</button>',
          '</div>',
          '<div style="font-size: 1.75rem; font-weight: 800; color: #3D2355; text-align: right;">' + lineTotalFormatted + '</div>',
          '<button type="button" onclick="meeekuRemovePageItem(\'' + itemKey + '\')" style="background: #FFF0F5; border: 1.5px solid #F8BBD0; color: #C2185B; border-radius: 50%; width: 34px; height: 34px; cursor: pointer; font-size: 1.3rem; display: flex; align-items: center; justify-content: center; transition: all 0.2s ease; flex-shrink: 0;" title="Remove from bag">✕</button>',
        '</div>',
      '</div>'
    ].join('');
  });

  list.innerHTML = html;
  var subtotalInRupees = Math.round((cart.total_price || 0) / 100);
  if (subtotalEl) {
    subtotalEl.innerText = formatMeeekuPaise(cart.total_price || 0);
  }
  updateMeeekuPageShippingProgress(subtotalInRupees);
  if (badgeEl) {
    badgeEl.innerText = (cart.item_count || 0) + (cart.item_count === 1 ? ' Item' : ' Items');
  }
  updateCartBadges(cart.item_count || 0);
}

function meeekuUpdatePageQty(itemKey, newQty) {
  var q = parseInt(newQty) || 0;
  
  // 1. Update local storage first
  var localCart = [];
  try {
    localCart = JSON.parse(localStorage.getItem('meeeku_active_cart') || localStorage.getItem('meeeku_user_cart') || localStorage.getItem('meeeku_cart') || '[]');
  } catch(e) {}

  var updatedLocal = [];
  localCart.forEach(function(item) {
    var key = item.key || item.id;
    if (String(key) === String(itemKey) || String(item.id) === String(itemKey)) {
      if (q > 0) {
        item.qty = q;
        item.quantity = q;
        updatedLocal.push(item);
      }
    } else {
      updatedLocal.push(item);
    }
  });
  try {
    var str = JSON.stringify(updatedLocal);
    localStorage.setItem('meeeku_active_cart', str);
    localStorage.setItem('meeeku_user_cart', str);
    localStorage.setItem('meeeku_cart', str);
  } catch(e) {}

  renderMeeekuPageCartWithLocalFallback();

  // 2. Sync to server
  fetch('/cart/change.js', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' },
    body: JSON.stringify({ id: String(itemKey), quantity: q })
  })
  .then(function(res) {
    if (!res.ok) return fetch('/cart.js', { cache: 'no-store' }).then(function(r) { return r.json(); });
    return res.json();
  })
  .then(function(cart) {
    renderMeeekuPageCartWithData(cart);
  })
  .catch(function() {
    renderMeeekuPageCartWithLocalFallback();
  });
}

function meeekuRemovePageItem(itemKey) {
  meeekuUpdatePageQty(itemKey, 0);
}

function meeekuClearCart() {
  if (confirm('Are you sure you want to clear your shopping bag?')) {
    try {
      localStorage.setItem('meeeku_active_cart', '[]');
      localStorage.setItem('meeeku_user_cart', '[]');
      localStorage.setItem('meeeku_cart', '[]');
    } catch(e) {}
    renderMeeekuPageCartWithLocalFallback();

    fetch('/cart/clear.js', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' }
    })
    .then(function() {
      fetchMeeekuPageCart();
    })
    .catch(function() {});
  }
}

function meeekuProceedToCheckout() {
  const btn = document.querySelector('.meeeku-btn-checkout');
  if (typeof meeekuGlobalProceedToCheckout === 'function') {
    meeekuGlobalProceedToCheckout(btn);
  } else {
    var discount = sessionStorage.getItem('meeeku_active_discount') || localStorage.getItem('meeeku_active_discount');
    var chkUrl = (discount && discount.trim())
      ? ('/discount/' + encodeURIComponent(discount.trim().toUpperCase()) + '?redirect=/checkout')
      : '/checkout';
    if (btn) {
      btn.setAttribute('disabled', 'true');
      btn.innerText = '⏳ Opening Checkout...';
    }
    fetch('/cart.js', { cache: 'no-store' })
      .then(function(res) { return res.json(); })
      .then(function(cart) {
        if (cart && cart.item_count > 0) {
          window.location.href = chkUrl;
        } else {
          if (btn) {
            btn.removeAttribute('disabled');
            btn.innerText = '🔒 Checkout';
          }
          if (typeof showMeeekuToast === 'function') showMeeekuToast('⚠️ Your bag is empty!');
        }
      })
      .catch(function() {
        window.location.href = chkUrl;
      });
  }
}

window.fetchMeeekuPageCart = fetchMeeekuPageCart;
window.meeekuUpdatePageQty = meeekuUpdatePageQty;
window.meeekuRemovePageItem = meeekuRemovePageItem;
window.meeekuClearCart = meeekuClearCart;

document.addEventListener('DOMContentLoaded', function() {
  fetchMeeekuPageCart();
  if (typeof meeekuSyncDiscountUI === 'function') meeekuSyncDiscountUI();
});

window.addEventListener('pageshow', function() {
  fetchMeeekuPageCart();
  const btn = document.querySelector('.meeeku-btn-checkout');
  if (btn) {
    btn.removeAttribute('disabled');
    btn.innerText = '🔒 Checkout';
  }
});
</script>

{% schema %}
{
  "name": "Meeeku Cart Items",
  "settings": [
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top Padding",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom Padding",
      "default": 48
    }
  ]
}
{% endschema %}
